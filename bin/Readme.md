This is pre-build for Debian 12 (bookworm)
==========================================

The `libxxhash` library is statically built into the `mariadb-xxhash.so` file.
Copy it to your plugins folder and activate the functions.
See the root Readme.md
